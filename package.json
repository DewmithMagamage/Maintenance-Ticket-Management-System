{
  "name": "bw-maintenance-monorepo",
  "private": true,
  "scripts": {
    "dev": "concurrently -n api,web -c blue,green \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "install:all": "npm install --prefix server && npm install --prefix client",
    "build:cpanel": "npm run build --prefix client && node server/scripts/copy-frontend.mjs"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
